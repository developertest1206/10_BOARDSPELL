# ── Web Framework ──────────────────────────────────────────
# FastAPI: the main backend framework
fastapi==0.115.0

# Uvicorn: the server that runs FastAPI
# [standard] includes extras like websockets and file watching
uvicorn[standard]==0.30.0

# ── Database ────────────────────────────────────────────────
# asyncpg: connects to PostgreSQL asynchronously
# FIX: bumped to 0.30.0 which supports Python 3.11 and builds correctly on Render
asyncpg==0.30.0

# databases: makes it easy to run async SQL queries
databases[asyncpg]==0.9.0

# SQLAlchemy: helps build SQL queries in Python
sqlalchemy==2.0.36

# ── Redis (task queue) ──────────────────────────────────────
# redis: connects to Redis for the automation event queue
redis==5.0.8

# ── HTTP Client ─────────────────────────────────────────────
# httpx: used to call the monday.com API
httpx==0.27.0

# ── Utilities ───────────────────────────────────────────────
# python-dotenv: reads variables from the .env file
python-dotenv==1.0.1

# python-multipart: needed for handling form data in FastAPI
python-multipart==0.0.9

# PyJWT: used to decode the monday.com access token
PyJWT==2.9.0