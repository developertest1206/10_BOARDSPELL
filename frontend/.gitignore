# Node modules — never push (reinstalled via npm install)
node_modules/

# Environment variables — contains API URLs and secrets
.env
.env.local
.env.development.local

# Build output
build/

# Logs
npm-debug.log*
yarn-debug.log*